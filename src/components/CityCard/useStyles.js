import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    wrapper: {
        padding: '24px',
        width: '200px'
    },
    cardStyle: {
        padding: '14px 0'
    },
    imageStyle: {
        display: 'block',
        margin: 'auto'
    },
    buttonStyle: {
        width: '45%'
    }
}))
