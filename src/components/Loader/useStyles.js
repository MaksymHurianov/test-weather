import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    wrapper: {
        margin: 'auto',
        display: 'block',
    },
    container: {
        height: '100%',
    },
    label: {
        color: "gray"
    }
}));
